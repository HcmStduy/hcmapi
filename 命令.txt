#pip freeze >requirements.txt
#pip install -r requirements.txt
创建管理员账号
python manage.py createsuperuser
迁移
python manage.py makemigrations
python manage.py migrate

python manage.py runserver

启动celery
celery -A QbuyPro worker -P eventlet -l info
celery -A advanceDjango worker -P eventlet -l info
python manage.py migrate django_celery_resulits
python manage.py migrate django_celery_beat
下载restful
pip install djangorestframework
pip install markdown       # 对可浏览 API 的 Markdown 支持。
pip install django-filter  # 过滤支持
